// Vercel Analytics is auto-enabled when deployed to Vercel
// Import and use the Analytics component in your root layout (App.jsx)
// No per-project configuration needed — it activates automatically upon deployment

export const initAnalytics = () => {
  // Vercel Analytics initialization happens via the React component
  // This is a placeholder for any custom analytics setup if needed
  console.log('Analytics initialized');
};
